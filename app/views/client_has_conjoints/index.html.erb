<p id="notice"><%= notice %></p>

<h1>Client Has Conjoints</h1>

<table>
  <thead>
    <tr>
      <th>Client</th>
      <th>Client has conjoint</th>
      <th>Datedebut</th>
      <th>Datefin</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @client_has_conjoints.each do |client_has_conjoint| %>
      <tr>
        <td><%= client_has_conjoint.Client %></td>
        <td><%= client_has_conjoint.Client_Has_Conjoint %></td>
        <td><%= client_has_conjoint.DateDebut %></td>
        <td><%= client_has_conjoint.DateFin %></td>
        <td><%= link_to 'Show', client_has_conjoint %></td>
        <td><%= link_to 'Edit', edit_client_has_conjoint_path(client_has_conjoint) %></td>
        <td><%= link_to 'Destroy', client_has_conjoint, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Client Has Conjoint', new_client_has_conjoint_path %>
